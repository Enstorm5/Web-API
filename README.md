COBSCCOMP251P-018

## Run locally

```bash
npm install
npm start
```

## Vercel

- The root route `/` returns JSON:
	- `status: "ok"`
	- `message: "session test"`
- Deploy with Vercel as a Node.js project using the included `vercel.json` rewrite.
